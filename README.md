1.	Please install requirements.py with the following command:
pip3 install -r requirements.txt 
2.	Run main.py to train the model from the data set.
3.	Run generate.py to generate a paragraph from the trained model
4.	Run cosine_sim.py to get the Spearman Rank Correlation
